{
  "name": "hagency-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 4328",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1 --port 4328",
    "check": "astro check",
    "test": "node --test tests/site.test.mjs",
    "verify": "npm run check && npm run build && npm test"
  },
  "dependencies": { "astro": "7.3.2" },
  "devDependencies": { "@astrojs/check": "0.9.10", "typescript": "^5.9.3", "playwright": "^1.58.2", "sharp": "^0.34.5", "@axe-core/playwright": "^4.10.2" }
}
